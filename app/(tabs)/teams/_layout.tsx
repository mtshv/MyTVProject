import { Stack } from "expo-router";

export default function TeamsStackLayout() {
  return (
    <Stack
      screenOptions={{
        headerShown: true,
        headerTitle: "Teams Stack",
      }}
    />
  );
}
